import React from 'react';
import './basic.css'; // 같은 폴더 안에 있는 'basic.css' 파일을 임포트

const Header = () => {
    return (
        <header>
            <h1>CRM-Project</h1>
            <p>고객 관계 관리 시스템</p>
        </header>
    );
};

export default Header;
